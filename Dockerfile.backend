FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml .
COPY src/ src/
COPY web/backend/ web/backend/

RUN pip install --no-cache-dir -e . && \
    pip install --no-cache-dir -e web/backend

EXPOSE 8000

CMD ["uvicorn", "web.backend.app:app", "--host", "0.0.0.0", "--port", "8000"]
